import { NextPage } from 'next';
import RoomDealerView from '~/containers/roomDealer';

interface Props {}

const Page: NextPage<Props> = ({}) => {
  return <RoomDealerView />;
};

export default Page;
